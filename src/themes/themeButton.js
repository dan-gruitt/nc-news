import { createTheme } from "@mui/material";

const buttonColor = createTheme({
    palette: {
      grey: {
        main: '#f6f6f6',
        light: '#fbfbfb',
        dark: '#dddddd',
        contrastText: '#595959',
      },
    },
  });

  export default buttonColor